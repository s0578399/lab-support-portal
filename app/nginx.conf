server {
  listen 80;
  root /usr/share/nginx/html;
  index index.html;

  location / {
    try_files $uri /index.html;
  }

  # Einziger API-Block: Pfad bleibt erhalten (/api/... → /api/...)
  location /api/ {
    proxy_pass http://backend:3000;   # <— kein trailing slash!
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-For $remote_addr;
    proxy_http_version 1.1;
  }
}
